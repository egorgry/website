# Move your existing resume Vite project's files here

package.json, src/, vite.config.js, index.html, etc. — same as your
current resume repo, just relocated into this folder.

Then add `base: '/resume/'` to vite.config.js (see the root README's
"Moving your resume in" section for why).

Delete this file once that's done.
